#include <tiny2313.h>
#include <delay.h>  
#include <stdio.h>
#include "project.h"

void main(void)
{                 
    init();
    linkDetected = 0;
    currentLightState = 0;
    
    while (1)
    {
        delay_ms(1000);
    }
}

